#include <iostream>
#include <queue>
using namespace std;

struct task {
    int id;
    int priority;

    bool operator< (const task& a, const task& b) return a.priority<b.proprity;
};

int main() {
    int n, m;
    while (cin >> n >> m) {
        
    }
}